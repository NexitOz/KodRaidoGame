REDIS_URL="redis://localhost:6379"
DATABASE_URL="postgresql://raido:raido@localhost:5432/kod_raido?schema=public"

# Phase 6 metrics providers — all optional. Unset means that provider is
# skipped entirely (no scraping fallback); each MediaAsset row still needs
# provider="youtube"/"tiktok"/"vk" + a valid externalId, same as the
# manual CSV import path, so the poller knows what to refresh.
YOUTUBE_API_KEY=
TIKTOK_ACCESS_TOKEN=
VK_ACCESS_TOKEN=
