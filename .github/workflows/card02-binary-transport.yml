name: Card02 Binary Transport

on:
  push:
    branches:
      - transport/card02-github-actions
    paths:
      - .github/workflows/card02-binary-transport.yml

permissions:
  contents: write

jobs:
  transport:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout transport branch
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Download exact master from firestorage file API and verify source bytes
        env:
          API_BASE: https://api.firestorage.ai/prod/file
          SHARE_ID: UbtC6RJp2_Ok
          EXPECTED_FILE_ID: 01a044b6f15c732c90c67a9d3375965e
          EXPECTED_NAME: seal-of-the-curse.webp
          EXPECTED_SIZE: "326508"
          EXPECTED_SHA256: 699db6b797effe04c2fd2b8642391af62da506d9e290374369bd842630258261
          EXPECTED_GIT_BLOB_SHA: 95940017577f7152a28bf76122912c37e548c7e0
        run: |
          set -euo pipefail
          python - <<'PY'
          import hashlib
          import json
          import os
          import urllib.request

          api = os.environ['API_BASE'].rstrip('/')
          share = os.environ['SHARE_ID']
          expected_file_id = os.environ['EXPECTED_FILE_ID']
          expected_name = os.environ['EXPECTED_NAME']
          expected_size = int(os.environ['EXPECTED_SIZE'])
          expected_sha = os.environ['EXPECTED_SHA256'].lower()
          expected_blob_sha = os.environ['EXPECTED_GIT_BLOB_SHA'].lower()
          headers = {'User-Agent':'Mozilla/5.0 Chrome/151','Accept':'application/json,*/*'}

          meta_url = f'{api}/shares/{share}/files?maxResults=1000'
          with urllib.request.urlopen(urllib.request.Request(meta_url, headers=headers), timeout=30) as r:
              meta = json.loads(r.read().decode('utf-8'))
          files = meta.get('files') or []
          matches = [f for f in files if f.get('fileId') == expected_file_id]
          if len(matches) != 1:
              raise SystemExit(f'Expected one fileId {expected_file_id}, found {len(matches)}: {files}')
          entry = matches[0]
          print('PROVIDER_FILE_ID=', entry.get('fileId'))
          print('PROVIDER_NAME=', entry.get('fileName'))
          print('PROVIDER_TYPE=', entry.get('contentType'))
          print('PROVIDER_SIZE=', entry.get('sizeBytes'))
          if entry.get('fileName') != expected_name:
              raise SystemExit('Provider filename mismatch')
          if entry.get('contentType') != 'image/webp':
              raise SystemExit('Provider MIME mismatch')
          if int(entry.get('sizeBytes', -1)) != expected_size:
              raise SystemExit('Provider size mismatch')

          dl_api = f'{api}/shares/{share}/files/{expected_file_id}/download'
          dl_req = urllib.request.Request(dl_api, data=b'', method='POST', headers=headers)
          with urllib.request.urlopen(dl_req, timeout=30) as r:
              payload = json.loads(r.read().decode('utf-8'))
          direct = payload.get('downloadUrl')
          if not direct:
              raise SystemExit(f'No downloadUrl from file API: {payload}')
          print('DOWNLOAD_URL_ISSUED=YES')

          file_req = urllib.request.Request(direct, headers={'User-Agent':'Mozilla/5.0 Chrome/151'})
          with urllib.request.urlopen(file_req, timeout=120) as r:
              data = r.read()
              print('DOWNLOAD_HTTP_STATUS=', getattr(r,'status',None))
              print('DOWNLOAD_CONTENT_TYPE=', r.headers.get('Content-Type'))
              print('DOWNLOAD_CONTENT_LENGTH=', r.headers.get('Content-Length'))

          size = len(data)
          sha = hashlib.sha256(data).hexdigest()
          blob_sha = hashlib.sha1(f'blob {size}\0'.encode('ascii') + data).hexdigest()
          riff_total = int.from_bytes(data[4:8], 'little') + 8 if len(data) >= 30 and data[:4] == b'RIFF' else -1
          fourcc = data[12:16] if len(data) >= 16 else b''
          width = height = -1
          if fourcc == b'VP8 ' and len(data) >= 30 and data[23:26] == b'\x9d\x01\x2a':
              width = int.from_bytes(data[26:28], 'little') & 0x3fff
              height = int.from_bytes(data[28:30], 'little') & 0x3fff

          print('MASTER_SIZE=', size)
          print('MASTER_SHA256=', sha)
          print('MASTER_GIT_BLOB_SHA=', blob_sha)
          print('MASTER_RIFF_TOTAL=', riff_total)
          print('MASTER_FOURCC=', repr(fourcc))
          print('MASTER_DIMENSIONS=', f'{width}x{height}')

          if size != expected_size: raise SystemExit(f'SIZE MISMATCH: {size} != {expected_size}')
          if sha != expected_sha: raise SystemExit(f'SHA256 MISMATCH: {sha} != {expected_sha}')
          if blob_sha != expected_blob_sha: raise SystemExit(f'GIT BLOB SHA MISMATCH: {blob_sha} != {expected_blob_sha}')
          if riff_total != expected_size: raise SystemExit(f'RIFF TOTAL MISMATCH: {riff_total} != {expected_size}')
          if fourcc != b'VP8 ': raise SystemExit(f'FOURCC MISMATCH: {fourcc!r}')
          if (width, height) != (1024,1536): raise SystemExit(f'DIMENSIONS MISMATCH: {(width,height)}')

          with open('/tmp/seal-of-the-curse.webp','wb') as f:
              f.write(data)
          print('SOURCE_BYTE_INTEGRITY=PASS')
          PY

          python -m pip install --quiet --disable-pip-version-check Pillow
          python - <<'PY'
          from PIL import Image
          p='/tmp/seal-of-the-curse.webp'
          with Image.open(p) as im:
              if im.format != 'WEBP':
                  raise SystemExit(f'FORMAT MISMATCH: {im.format}')
              if im.size != (1024,1536):
                  raise SystemExit(f'DECODE DIMENSIONS MISMATCH: {im.size}')
              im.load()
              print('DECODE_FORMAT=', im.format)
              print('DECODE_DIMENSIONS=', f'{im.width}x{im.height}')
          print('SOURCE_FULL_DECODE=PASS')
          PY

      - name: Create candidate-v2 from fresh main and verify Git object before and after push
        env:
          EXPECTED_SIZE: "326508"
          EXPECTED_SHA256: 699db6b797effe04c2fd2b8642391af62da506d9e290374369bd842630258261
          EXPECTED_GIT_BLOB_SHA: 95940017577f7152a28bf76122912c37e548c7e0
          TARGET_BRANCH: assets/seal-of-the-curse-candidate-v2
          TARGET_PATH: art-source/seal-of-the-curse.webp
        run: |
          set -euo pipefail
          git fetch origin main
          if git ls-remote --exit-code --heads origin "$TARGET_BRANCH" >/dev/null 2>&1; then
            echo "Target branch already exists; refusing to overwrite it."
            exit 1
          fi

          git switch --detach origin/main
          git switch -c "$TARGET_BRANCH"
          mkdir -p art-source
          cp /tmp/seal-of-the-curse.webp "$TARGET_PATH"

          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add "$TARGET_PATH"
          git commit -m "art: add exact Seal of the Curse candidate v2"

          LOCAL_SIZE="$(git cat-file -s "HEAD:$TARGET_PATH")"
          LOCAL_SHA="$(git show "HEAD:$TARGET_PATH" | sha256sum | awk '{print $1}')"
          LOCAL_BLOB="$(git rev-parse "HEAD:$TARGET_PATH")"
          echo "LOCAL_GIT_SIZE=$LOCAL_SIZE"
          echo "LOCAL_GIT_SHA256=$LOCAL_SHA"
          echo "LOCAL_GIT_BLOB_SHA=$LOCAL_BLOB"
          test "$LOCAL_SIZE" = "$EXPECTED_SIZE"
          test "$LOCAL_SHA" = "$EXPECTED_SHA256"
          test "$LOCAL_BLOB" = "$EXPECTED_GIT_BLOB_SHA"

          git push origin "HEAD:refs/heads/$TARGET_BRANCH"
          git fetch origin "refs/heads/$TARGET_BRANCH:refs/remotes/origin/$TARGET_BRANCH"

          REMOTE_SIZE="$(git cat-file -s "origin/$TARGET_BRANCH:$TARGET_PATH")"
          REMOTE_SHA="$(git show "origin/$TARGET_BRANCH:$TARGET_PATH" | sha256sum | awk '{print $1}')"
          REMOTE_BLOB="$(git rev-parse "origin/$TARGET_BRANCH:$TARGET_PATH")"
          echo "REMOTE_GIT_SIZE=$REMOTE_SIZE"
          echo "REMOTE_GIT_SHA256=$REMOTE_SHA"
          echo "REMOTE_GIT_BLOB_SHA=$REMOTE_BLOB"
          test "$REMOTE_SIZE" = "$EXPECTED_SIZE"
          test "$REMOTE_SHA" = "$EXPECTED_SHA256"
          test "$REMOTE_BLOB" = "$EXPECTED_GIT_BLOB_SHA"
          echo "CANDIDATE_V2_REMOTE_INTEGRITY=PASS"
